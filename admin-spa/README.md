admin-spa — Vite React Admin Dashboard

To run locally:
1. cd admin-spa
2. npm install
3. npm run dev

This scaffold includes a minimal router and pages for Login and Add Property. It will be connected to backend-api for auth and property creation.
